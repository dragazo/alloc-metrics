name: CI

on:
  push:
  pull_request:

jobs:
  doc:
    name: doc
    runs-on: ubuntu-latest
    strategy:
      matrix:
        thread: ["", "thread"]
        global: ["", "global"]
    env:
      RUSTDOCFLAGS: "-D warnings"
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - uses: actions-rs/cargo@v1
        with:
          command: doc
          args: --no-deps --no-default-features --features ${{ matrix.thread }},${{ matrix.global }}

  test:
    name: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        opt: ["", "--release"]
        thread: ["", "thread"]
        global: ["", "global"]
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - uses: actions-rs/cargo@v1
        with:
          command: test
          args: --lib ${{ matrix.opt }} --no-default-features --features ${{ matrix.thread }},${{ matrix.global }}

  audit:
    name: audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - run: cargo install cargo-audit
      - run: cargo generate-lockfile
      - run: cargo audit --deny warnings

  no-std:
    name: no-std
    runs-on: ubuntu-latest
    strategy:
      matrix:
        opt: ["", "--release"]
        global: ["", "global"]
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - uses: actions-rs/cargo@v1
        with:
          use-cross: true
          command: build
          args: --lib ${{ matrix.opt }} --no-default-features --features ${{ matrix.global }} --target thumbv6m-none-eabi
